```text
Overall verdict:

TC-1A v0.3.2 is a successful semantic-enforcement patch. It closes the specific structural and definitional cracks identified in v0.3.1 without claiming false resolution of the underlying physics debts. It renders the protocol ready for a draft specification of the executable check.

Does v0.3.2 close the v0.3.1 gaps?

Yes. The patch addresses every listed gap by either providing a definition, an enforcement rule, or an explicit debt acknowledgment where a full definition is impossible at this stage.

Remaining undefined terms:

None critical. `RelState` and `pi` (in `CoarseGraining`) are carried forward as assumed types from the wider TBA context, but their interface contracts are sufficiently specified for the TC-1B kernel. `WitnessLabel` is defined in the Go guardrails section.

Remaining hidden imports:

No new hidden imports detected. The patch explicitly surfaces `metadataTruthDebt`, `thresholdJustificationDebt`, and `finiteRecurrenceDebt` which were previously implicit or missing.

Endpoint / CoarseAlternatives assessment:

Both are now sharply defined.
`endpoint` is defined via `Nat.iterate`, which is deterministic and Go-implementable.
`CoarseAlternatives` is defined as a proposition combining inequality of histories with equality of coarse endpoints. The explicit "Forbidden inference" block prevents immediate over-interpretation as quantum phenomena.

Canonical encoding assessment:

The rules are now deterministic (lexicographic sort, explicit escaping, argument sorting for unordered relations). The admission of `fullGraphIsomorphismDebt` is EBP-compliant—it solves the reproducibility problem for TC-1B without claiming to solve the graph isomorphism problem generally.

Metadata enforcement assessment:

Handled honestly. The patch correctly distinguishes between "valid Go execution" and "truth of metadata." By renaming Bool fields to "audit declarations" and attaching `metadataTruthDebt`, it prevents the self-reporting loophole from silently becoming proof. The runtime rejection of invalid combinations (e.g., `handPainted = true`) adds necessary guardrails.

History selection assessment:

The introduction of `StartSelectionMode` and the `allStates` default closes the "hand-selected start" loophole. The `maxLength` rule (fixed before witness, recommended `|S|`) closes the "tune-to-fit" loophole. The `history_selection_debt_open` status provides a valid downgrade path for non-default modes.

Null-model threshold assessment:

The thresholds (0.25 for random, 0.10 for permutation) are pragmatic and explicitly flagged as `thresholdJustificationDebt`. They are strict enough to filter noise but do not pretend to be physical constants. The requirements for minimum trial counts (100/1000) prevent statistical underpowered claims.

Parity/XOR status assessment:

The three-status system (downgrade, survives_with_debt, survived) is precise. It correctly handles the case where parity explains the arithmetic but not the mechanism. The separation of "arithmetic pattern" from "substrate mechanism" is a crucial semantic defense against claiming success by trivial means.

U(1) / phase boundary assessment:

The boundary audit schema is sufficient for TC-1B. By explicitly requiring the JSON report to deny `continuous_phase_used`, `complex_numbers_used`, etc., it creates a hard fence against smuggling Hilbert space concepts into the finite toy model.

Go type-guardrail assessment:

Sufficient. The prohibition of type aliases to `Probability` or `Amplitude` is a strong semantic firewall. The distinct types for `UnlabeledCount` and `SignedTally` ensure that domain boundaries are respected in the implementation.

JSON report schema assessment:

Complete for the intended purpose. The addition of `runtime_validation`, `boundary_audit`, `null_model_power`, and `selection_audit` sections covers the new enforcement mechanisms. It provides enough data to audit a run remotely.

Should TC-1B be drafted next?

Yes. "draft TC-1B spec."

Required edits before TC-1B:

None. The specification in v0.3.2 is sufficiently tight to serve as the requirements document for the TC-1B draft.

Smallest useful next move:

Draft the TC-1B specification document, instantiating the concrete types (`MicroSpace`, `ReversibleUpdate`, `CoarseGraining`) and defining the exact CLI entry point for the toy check.
```
